{
  "name": "@forza-telemetry/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.mjs --configLoader native --host 0.0.0.0 --port 5173",
    "build": "tsc -p tsconfig.json && vite build --config vite.config.mjs --configLoader native",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview --config vite.config.mjs --configLoader native --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "@types/react": "^18.3.17",
    "@types/react-dom": "^18.3.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
